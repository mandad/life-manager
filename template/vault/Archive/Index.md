# Archive

Items moved out of active rotation — completed tasks, superseded plans, closed projects. Mirrors the active layout (`Archive/Documents/`, `Archive/Projects/`, `Archive/Notes/`) so Obsidian search still finds everything.

Every archived file carries `archived: YYYY-MM-DD` + `archived_reason:` in its frontmatter, and gets one line here:

## Catalog

*(nothing archived yet)*
