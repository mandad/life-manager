---
title: Automation backlog
purpose: Running list of automation candidates Claude has proposed — repetitive or mechanical work that scripts, scheduled agents, or hooks could take off the user's plate
maintained_by: "[[Daily update routine]] Step 9 + [[Weekly review routine]] Step 9"
status_legend: "Proposed → user marks Approved → Claude builds on next run → moves to Built / running"
---

# Automation backlog

Claude proposes candidates here whenever a routine surfaces work that's mechanical and computer-bound. The user reviews. **Check a box under "Approved" to authorize Claude to build it on the next run.** Decline by moving to "Declined" with a short why — declined ideas are never re-proposed.

Mechanisms in play: **script** (Python/Bash invoked during a routine) · **scheduled agent** (cron) · **hook** (fires on tool/event) · **skill** (invokable on demand) · **MCP** (connector to an API).

---

## Proposed (awaiting decision)

*(none yet — the routines will populate this)*

---

## Approved — build on next run

*(check a box to authorize; Claude keeps every still-Proposed item enumerated here as an unchecked line so approval is one click)*

---

## Built / running

*(each entry: where it lives, what it does, verification note, source proposal)*

---

## Declined

*(each entry: the user's reason — respected permanently)*
